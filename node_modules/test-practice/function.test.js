// const {sum} = require('./index');

// describe('test', () => {
//     describe('testing', () => {
//         test ('now', () => {
//             expect(sum(1,2)).toBe(3)
//         })
//     })
// })


const {generateText} = require('./functionFile.js');

test('should output name and age', () => {
    const text = generateText('Max', 29);
    expect(text).toBe('Max (29 years old)');
})
